<!DOCTYPE html>
<html lang="en" class="scroll-smooth">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Top Voices Workbook - Essential Insights from Expert Conversations</title>
    <link href="/styles.css" rel="stylesheet">
    <link rel="icon" type="image/svg+xml" href="/favicon.svg">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/11.9.0/styles/github-dark.min.css" rel="stylesheet">
    <script src="https://unpkg.com/lucide@latest/dist/umd/lucide.js"></script>
    <meta name="description" content="Extract the most valuable insights from conversations with industry leaders. Structured workbooks featuring wisdom from top voices in marketing, business, and entrepreneurship.">
    
    <!-- Open Graph Meta Tags -->
    <meta property="og:title" content="Top Voices Workbook - Essential Insights from Expert Conversations">
    <meta property="og:description" content="Extract the most valuable insights from conversations with industry leaders. Structured workbooks featuring wisdom from top voices in marketing, business, and entrepreneurship.">
    <meta property="og:type" content="website">
    <meta property="og:url" content="https://www.topvoicesworkbook.com/">
    <meta property="og:image" content="https://www.topvoicesworkbook.com/og-image.png?v=2">
    <meta property="og:image:width" content="1200">
    <meta property="og:image:height" content="630">
    <meta property="og:image:alt" content="Top Voices Workbook - Educational Content Platform">
    <meta property="og:site_name" content="Top Voices Workbook">
    <meta property="og:locale" content="en_US">
    
    <!-- Twitter Card Meta Tags -->
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content="Top Voices Workbook - Essential Insights from Expert Conversations">
    <meta name="twitter:description" content="Extract the most valuable insights from conversations with industry leaders. Structured workbooks featuring wisdom from top voices in marketing, business, and entrepreneurship.">
    <meta name="twitter:image" content="https://www.topvoicesworkbook.com/og-image.png">
    <meta name="twitter:image:alt" content="Top Voices Workbook - Educational Content Platform">
    
    <!-- Google Search Console Verification -->
    <meta name="google-site-verification" content="Ofv-opHiVNnx7W_OTP3GFFnu2puSwzkple8weWgtc2s">
    
    <script>
      window.va = window.va || function () { (window.vaq = window.vaq || []).push(arguments); };
    </script>
    <script defer src="/_vercel/insights/script.js"></script>
    <script defer src="/_vercel/speed-insights/script.js"></script>
</head>
<body class="min-h-screen bg-background">
    <!-- Navigation -->
    <nav class="sticky top-0 z-50 border-b bg-background/95 backdrop-blur supports-[backdrop-filter]:bg-background/60">
        <div class="container mx-auto px-4">
            <div class="flex h-16 items-center justify-between">
                <div class="flex items-center space-x-4">
                    <a href="/" class="flex items-center space-x-2 font-heading font-bold text-xl">
                        <div class="w-8 h-8 bg-primary rounded-lg flex items-center justify-center">
                            <i data-lucide="bookmark" class="w-4 h-4 text-primary-foreground"></i>
                        </div>
                        <span>Top Voices Workbook</span>
                    </a>
                </div>
                
                <div class="flex items-center space-x-4">
                    <button id="theme-toggle" class="btn-outline p-2">
                        <i data-lucide="sun" class="w-4 h-4"></i>
                    </button>
                </div>
            </div>
        </div>
    </nav>

<!-- Hero Section -->
<section class="relative overflow-hidden bg-gradient-to-br from-background via-background to-muted/20">
    
    <!-- Content -->
    <div class="relative z-10 container mx-auto px-4 py-20">
        <div class="grid grid-cols-1 lg:grid-cols-2 gap-6 lg:gap-12 items-center">
            <!-- Left side - Text Content -->
            <div class="animate-on-scroll">
                <div class="badge bg-primary/10 text-primary border-primary/20 mb-6">
                    📚 Educational Content Platform
                </div>
                    
                <h1 class="text-2xl sm:text-3xl md:text-4xl lg:text-6xl font-heading font-bold leading-tight mb-6">
                    Learn from
                    <span class="text-transparent bg-clip-text bg-gradient-to-r from-primary to-blue-600">
                        Top Voices
                    </span>
                </h1>
                    
                <p class="text-base sm:text-lg lg:text-xl text-muted-foreground mb-8 leading-relaxed">
                    Transform lengthy educational videos into structured, readable workbooks. Access comprehensive notes 
                    from marketing legends and industry experts covering branding, psychology, and business strategy.
                </p>
                
                <div class="flex flex-wrap items-center gap-6 text-sm text-muted-foreground mb-8">
                    <div class="flex items-center gap-2">
                        <i data-lucide="book-open" class="w-4 h-4"></i>
                        <span><%= workbooks.length %> Workbooks</span>
                    </div>
                    <div class="flex items-center gap-2">
                        <i data-lucide="users" class="w-4 h-4"></i>
                        <span>Expert Authors</span>
                    </div>
                    <div class="flex items-center gap-2">
                        <i data-lucide="zap" class="w-4 h-4"></i>
                        <span>Actionable Content</span>
                    </div>
                </div>
                
                <div class="flex flex-col sm:flex-row gap-3 sm:gap-4 relative z-30">
                    <% if (workbooks.length > 0) { %>
                    <a href="/course/<%= workbooks[0].slug %>/1" class="btn-primary text-base sm:text-lg h-12 sm:h-14 px-6 sm:px-10 relative z-30 justify-center">
                        <i data-lucide="book-open" class="w-4 sm:w-5 h-4 sm:h-5 mr-2"></i>
                        Start Learning
                    </a>
                    <% } %>
                    <a href="#workbooks" class="btn-outline text-base sm:text-lg h-12 sm:h-14 px-6 sm:px-10 relative z-30 justify-center">
                        <i data-lucide="list" class="w-4 sm:w-5 h-4 sm:h-5 mr-2"></i>
                        Browse Workbooks
                    </a>
                </div>
            </div>
            
            <!-- Right side - Workbook Preview -->
            <div class="hidden lg:flex justify-center items-center animate-on-scroll">
                <div class="relative">
                    <!-- Main Preview Card -->
                    <div class="card p-6 w-80 bg-gradient-to-br from-background to-muted/20 shadow-2xl border-2 border-primary/10 transform rotate-3 hover:rotate-0 transition-all duration-500">
                        <div class="flex items-center gap-3 mb-4">
                            <div class="w-10 h-10 bg-primary rounded-lg flex items-center justify-center">
                                <i data-lucide="bookmark" class="w-5 h-5 text-primary-foreground"></i>
                            </div>
                            <div>
                                <h3 class="font-heading font-bold text-sm">Top Voices Workbook</h3>
                                <p class="text-xs text-muted-foreground">Structured Learning</p>
                            </div>
                        </div>
                        
                        <!-- Mini Chapter Preview -->
                        <div class="space-y-2 mb-4">
                            <% if (workbooks && workbooks.length > 0) { %>
                            <div class="flex items-center gap-2 text-xs text-muted-foreground">
                                <div class="w-4 h-4 bg-primary/10 rounded flex items-center justify-center">
                                    <span class="text-[10px] font-bold text-primary">1</span>
                                </div>
                                <span class="truncate">Introduction & Fundamentals</span>
                            </div>
                            <div class="flex items-center gap-2 text-xs text-muted-foreground">
                                <div class="w-4 h-4 bg-primary/10 rounded flex items-center justify-center">
                                    <span class="text-[10px] font-bold text-primary">2</span>
                                </div>
                                <span class="truncate">Strategic Framework</span>
                            </div>
                            <div class="flex items-center gap-2 text-xs text-muted-foreground">
                                <div class="w-4 h-4 bg-primary/10 rounded flex items-center justify-center">
                                    <span class="text-[10px] font-bold text-primary">3</span>
                                </div>
                                <span class="truncate">Implementation Guide</span>
                            </div>
                            <div class="text-xs text-muted-foreground pl-6">...and <%= workbooks.reduce((sum, wb) => sum + wb.totalChapters, 0) - 3 %> more chapters</div>
                            <% } %>
                        </div>
                        
                        <!-- Stats -->
                        <div class="flex items-center justify-between text-xs text-muted-foreground border-t pt-3">
                            <div class="flex items-center gap-1">
                                <i data-lucide="users" class="w-3 h-3"></i>
                                <span><%= workbooks ? workbooks.length : 0 %> Authors</span>
                            </div>
                            <div class="flex items-center gap-1">
                                <i data-lucide="clock" class="w-3 h-3"></i>
                                <span>Hours of Content</span>
                            </div>
                        </div>
                    </div>
                    
                    <!-- Background Cards for Depth -->
                    <div class="absolute -top-2 -left-2 w-80 h-full bg-gradient-to-br from-primary/5 to-blue-500/5 rounded-lg -z-10 transform rotate-6"></div>
                    <div class="absolute -top-4 -left-4 w-80 h-full bg-gradient-to-br from-muted/30 to-muted/10 rounded-lg -z-20 transform rotate-12"></div>
                    
                    <!-- Floating Elements -->
                    <div class="absolute -top-8 right-4 w-16 h-16 bg-primary/10 rounded-full flex items-center justify-center animate-pulse">
                        <i data-lucide="zap" class="w-8 h-8 text-primary"></i>
                    </div>
                    
                    <div class="absolute -bottom-6 -right-6 w-12 h-12 bg-green-500/10 rounded-full flex items-center justify-center">
                        <i data-lucide="check-circle" class="w-6 h-6 text-green-500"></i>
                    </div>
                    
                    <!-- Stats Bubbles -->
                    <div class="absolute top-1/2 -left-12 transform -translate-y-1/2">
                        <div class="bg-background/90 backdrop-blur-sm border border-primary/20 rounded-lg p-2 text-center shadow-lg">
                            <div class="text-lg font-bold text-primary"><%= workbooks ? workbooks.reduce((sum, wb) => sum + wb.totalChapters, 0) : '50+' %></div>
                            <div class="text-xs text-muted-foreground">Chapters</div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    
    <!-- Decorative elements -->
    <div class="absolute top-20 right-20 w-72 h-72 bg-primary/5 rounded-full blur-3xl"></div>
    <div class="absolute bottom-20 left-20 w-96 h-96 bg-blue-500/5 rounded-full blur-3xl"></div>
</section>

<!-- Available Workbooks Section -->
<section id="workbooks" class="py-20 bg-muted/30">
    <div class="container mx-auto px-4">
        <div class="text-center mb-16 animate-on-scroll">
            <h2 class="text-2xl sm:text-3xl lg:text-4xl font-heading font-bold mb-4">
                Available Workbooks
            </h2>
            <p class="text-base sm:text-lg lg:text-xl text-muted-foreground max-w-2xl mx-auto">
                Choose from our collection of expertly curated workbooks, each transforming hours of video content into structured learning experiences
            </p>
        </div>
        
        <div class="grid grid-cols-1 lg:grid-cols-2 gap-8 stagger-in">
            <% workbooks.forEach((workbook, index) => { %>
            <div class="card p-8 animate-on-scroll hover:shadow-xl transition-all duration-300 group relative overflow-hidden">
                <!-- Workbook Background Images -->
                <% if (workbook.slug === 'personal-branding-caleb-ralston') { %>
                <!-- Caleb's Personal Branding workbook background -->
                <div class="absolute inset-0 z-0">
                    <img src="/caleb-photo.webp" alt="<%= workbook.author %>" class="w-full h-full object-cover object-top">
                </div>
                <!-- Strong overlay for text readability -->
                <div class="absolute inset-0 z-10 bg-gradient-to-br from-background/95 via-background/90 to-background/85"></div>
                <% } else if (workbook.slug === 'magnetic-brand-seth-godin') { %>
                <!-- Seth Godin's workbook background -->
                <div class="absolute top-[25px] right-0 w-[75%] h-[75%] z-0" style="transform: translateX(200px);">
                    <img src="/seth-godin-photo.png" alt="<%= workbook.author %>" class="w-full h-full object-cover object-top">
                </div>
                <!-- Strong overlay for text readability -->
                <div class="absolute inset-0 z-10 bg-gradient-to-br from-background/95 via-background/90 to-background/85"></div>
                <% } %>
                
                <!-- Workbook Header -->
                <div class="flex items-start justify-between mb-6 relative z-20">
                    <div class="flex-1">
                        <div class="flex items-center gap-1.5 mb-3 overflow-hidden">
                            <% workbook.tags.slice(0, 4).forEach(tag => { %>
                            <span class="inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium bg-primary/10 text-primary whitespace-nowrap">
                                <%= tag %>
                            </span>
                            <% }); %>
                            <span class="inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium bg-muted text-muted-foreground whitespace-nowrap">
                                <%= workbook.difficulty %>
                            </span>
                        </div>
                        <h3 class="font-heading font-bold text-xl mb-2 group-hover:text-primary transition-colors">
                            <%= workbook.title %>
                        </h3>
                        <% if (workbook.subtitle) { %>
                        <p class="text-sm text-muted-foreground mb-3"><%= workbook.subtitle %></p>
                        <% } %>
                        <div class="flex items-center gap-2 mb-4">
                            <span class="text-sm text-muted-foreground">by</span>
                            <a href="<%= workbook.authorWebsite %>" target="_blank" rel="noopener noreferrer" 
                               class="text-sm font-medium text-primary hover:text-primary/80 transition-colors">
                                <%= workbook.author %>
                            </a>
                            <a href="<%= workbook.authorWebsite %>" target="_blank" rel="noopener noreferrer" 
                               class="text-muted-foreground hover:text-primary transition-colors">
                                <i data-lucide="external-link" class="w-3 h-3"></i>
                            </a>
                        </div>
                    </div>
                </div>
                
                <!-- Workbook Description -->
                <p class="text-muted-foreground mb-6 line-clamp-3 relative z-20">
                    <%= workbook.description %>
                </p>
                
                <!-- Workbook Stats -->
                <div class="flex flex-wrap items-center gap-6 text-sm text-muted-foreground mb-6 relative z-20">
                    <div class="flex items-center gap-2">
                        <i data-lucide="book-open" class="w-4 h-4"></i>
                        <span><%= workbook.totalChapters %> Chapters</span>
                    </div>
                    <div class="flex items-center gap-2">
                        <i data-lucide="clock" class="w-4 h-4"></i>
                        <span><%= workbook.duration %></span>
                    </div>
                    <div class="flex items-center gap-2">
                        <i data-lucide="layers" class="w-4 h-4"></i>
                        <span><%= Object.keys(workbook.sections).length %> Sections</span>
                    </div>
                </div>
                
                <!-- Sample Chapters -->
                <div class="mb-6 relative z-20">
                    <h4 class="font-semibold text-sm mb-3 text-muted-foreground">PREVIEW CHAPTERS</h4>
                    <div class="space-y-2">
                        <% workbook.firstChapters.forEach(chapter => { %>
                        <div class="flex items-center gap-3 text-sm">
                            <div class="w-6 h-6 bg-primary/10 rounded-md flex items-center justify-center flex-shrink-0">
                                <span class="text-xs font-bold text-primary"><%= chapter.id %></span>
                            </div>
                            <span class="text-muted-foreground truncate"><%= chapter.title %></span>
                        </div>
                        <% }); %>
                        <% if (workbook.totalChapters > 3) { %>
                        <div class="text-xs text-muted-foreground pl-9">
                            ...and <%= workbook.totalChapters - 3 %> more chapters
                        </div>
                        <% } %>
                    </div>
                </div>
                
                <!-- Action Buttons -->
                <div class="flex flex-col sm:flex-row gap-3 relative z-20">
                    <a href="/course/<%= workbook.slug %>/1" 
                       class="btn-primary text-sm h-10 px-4 justify-center flex-1">
                        <i data-lucide="play" class="w-4 h-4 mr-2"></i>
                        Start Reading
                    </a>
                    <a href="/course/<%= workbook.slug %>" 
                       class="btn-outline text-sm h-10 px-4 justify-center">
                        <i data-lucide="list" class="w-4 h-4 mr-2"></i>
                        View Contents
                    </a>
                </div>
                
                <!-- Source Attribution -->
                <div class="mt-4 pt-3 border-t border-muted/20 relative z-20">
                    <div class="flex items-center justify-between text-xs text-muted-foreground">
                        <div class="flex items-center gap-1">
                            <i data-lucide="video" class="w-3 h-3"></i>
                            <span>Based on original content</span>
                        </div>
                        <% if (workbook.sourceVideo) { %>
                        <a href="<%= workbook.sourceVideo %>" target="_blank" rel="noopener noreferrer"
                           class="flex items-center gap-1 hover:text-red-500 transition-colors">
                            <i data-lucide="youtube" class="w-3 h-3"></i>
                            <span>Watch Original</span>
                        </a>
                        <% } %>
                    </div>
                </div>
                
                <!-- Background decoration -->
                <div class="absolute top-4 right-4 w-16 h-16 bg-primary/5 rounded-full blur-xl opacity-0 group-hover:opacity-100 transition-opacity"></div>
            </div>
            <% }); %>
        </div>
    </div>
</section>

<!-- Platform Features -->
<section class="py-20">
    <div class="container mx-auto px-4">
        <div class="text-center mb-16 animate-on-scroll">
            <h2 class="text-2xl sm:text-3xl lg:text-4xl font-heading font-bold mb-4">
                Why Choose Our Workbooks?
            </h2>
            <p class="text-base sm:text-lg lg:text-xl text-muted-foreground max-w-2xl mx-auto">
                We transform lengthy video content into structured, actionable learning experiences
            </p>
        </div>
        
        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6 lg:gap-8">
            <div class="text-center animate-on-scroll">
                <div class="w-16 h-16 bg-primary/10 rounded-full flex items-center justify-center mx-auto mb-6">
                    <i data-lucide="zap" class="w-8 h-8 text-primary"></i>
                </div>
                <h3 class="font-heading font-bold text-xl mb-4">Time-Saving Format</h3>
                <p class="text-muted-foreground">
                    Skip the video scrubbing. Get straight to the insights with structured, searchable notes.
                </p>
            </div>
            
            <div class="text-center animate-on-scroll">
                <div class="w-16 h-16 bg-primary/10 rounded-full flex items-center justify-center mx-auto mb-6">
                    <i data-lucide="brain" class="w-8 h-8 text-primary"></i>
                </div>
                <h3 class="font-heading font-bold text-xl mb-4">Expert Curation</h3>
                <p class="text-muted-foreground">
                    Learn from industry legends and top voices who have built successful businesses and brands.
                </p>
            </div>
            
            <div class="text-center animate-on-scroll">
                <div class="w-16 h-16 bg-primary/10 rounded-full flex items-center justify-center mx-auto mb-6">
                    <i data-lucide="bookmark" class="w-8 h-8 text-primary"></i>
                </div>
                <h3 class="font-heading font-bold text-xl mb-4">Structured Learning</h3>
                <p class="text-muted-foreground">
                    Content organized into logical chapters and sections for progressive learning and easy reference.
                </p>
            </div>
        </div>
    </div>
</section>

<!-- CTA Section -->
<section class="py-20 bg-gradient-to-br from-primary/5 to-blue-500/5">
    <div class="container mx-auto px-4 text-center">
        <div class="max-w-2xl mx-auto animate-on-scroll">
            <h2 class="text-2xl sm:text-3xl lg:text-4xl font-heading font-bold mb-6">
                Ready to Start Learning?
            </h2>
            <p class="text-base sm:text-lg lg:text-xl text-muted-foreground mb-8">
                Join others who are transforming their knowledge with structured, actionable content from industry experts.
            </p>
            <div class="flex flex-col sm:flex-row gap-3 sm:gap-4 justify-center">
                <% if (workbooks.length > 0) { %>
                <a href="/course/<%= workbooks[0].slug %>/1" class="btn-primary text-base sm:text-lg h-12 sm:h-14 px-6 sm:px-8 justify-center">
                    <i data-lucide="book-open" class="w-4 sm:w-5 h-4 sm:h-5 mr-2"></i>
                    Start with <%= workbooks[0].author %>'s Workbook
                </a>
                <% } %>
                <a href="#workbooks" class="btn-outline text-base sm:text-lg h-12 sm:h-14 px-6 sm:px-8 justify-center">
                    <i data-lucide="eye" class="w-4 sm:w-5 h-4 sm:h-5 mr-2"></i>
                    Browse All Workbooks
                </a>
            </div>
        </div>
    </div>
</section>

    <!-- Footer -->
    <footer class="border-t bg-muted/50 mt-20">
        <div class="container mx-auto px-4 py-12">
            <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
                <div>
                    <h3 class="font-heading font-bold text-lg mb-4">Top Voices Workbook</h3>
                    <p class="text-muted-foreground text-sm">
                        Transform lengthy educational videos into structured, actionable learning experiences. 
                        Learn from industry experts and marketing legends.
                    </p>
                </div>
                <div>
                    <h4 class="font-semibold mb-4 text-primary">Available Workbooks</h4>
                    <div class="space-y-2 text-sm">
                        <% workbooks.forEach(workbook => { %>
                        <a href="/course/<%= workbook.slug %>" class="block text-muted-foreground hover:text-foreground transition-colors">
                            <%= workbook.author %>: <%= workbook.title %>
                        </a>
                        <% }); %>
                    </div>
                </div>
                <div>
                    <h4 class="font-semibold mb-4 text-primary">Quick Links</h4>
                    <div class="space-y-2 text-sm">
                        <a href="/" class="block text-muted-foreground hover:text-foreground transition-colors">Home</a>
                        <a href="#workbooks" class="block text-muted-foreground hover:text-foreground transition-colors">Browse Workbooks</a>
                        <a href="/api/workbooks" class="block text-muted-foreground hover:text-foreground transition-colors">API</a>
                    </div>
                </div>
            </div>
            
            <div class="border-t mt-12 pt-8 text-center text-sm text-muted-foreground">
                <p>Built with 🚀 Node.js, Tailwind CSS & shadcn/ui</p>
            </div>
        </div>
    </footer>

    <script>
        // Initialize Lucide icons
        lucide.createIcons();
        
        // Theme toggle
        const themeToggle = document.getElementById('theme-toggle');
        const html = document.documentElement;
        
        themeToggle?.addEventListener('click', () => {
            html.classList.toggle('dark');
            const icon = themeToggle.querySelector('[data-lucide]');
            icon.setAttribute('data-lucide', html.classList.contains('dark') ? 'moon' : 'sun');
            lucide.createIcons();
            
            // Save preference
            localStorage.setItem('theme', html.classList.contains('dark') ? 'dark' : 'light');
        });
        
        // Load saved theme
        const savedTheme = localStorage.getItem('theme');
        if (savedTheme === 'dark' || (!savedTheme && window.matchMedia('(prefers-color-scheme: dark)').matches)) {
            html.classList.add('dark');
            const icon = themeToggle?.querySelector('[data-lucide]');
            if (icon) {
                icon.setAttribute('data-lucide', 'moon');
                lucide.createIcons();
            }
        }
        
        // Smooth scrolling for anchor links
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({
                        behavior: 'smooth',
                        block: 'start'
                    });
                }
            });
        });
        
        // Add animation to elements as they come into view
        const observerOptions = {
            threshold: 0.1,
            rootMargin: '0px 0px -50px 0px'
        };
        
        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('animate-in');
                }
            });
        }, observerOptions);
        
        // Observe elements with animation classes
        document.querySelectorAll('.animate-on-scroll').forEach(el => {
            observer.observe(el);
        });
    </script>
</body>
</html>